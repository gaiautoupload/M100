# M100 public static dashboard

This repository intentionally contains only the public static frontend, its display assets, and the dashboard JSON snapshot.

The browser passcode is a UI-only gate. It does not protect the static files or data.

The repository excludes strategy materials, raw transaction files, source credentials, server-side login code, and local update automation.
